---
name: 'AWS deploy'
description: 'Deploy the application onto AWS ECS'
inputs:
  name:
    required: true
    description: 'Name of the project which will be used to reference and name the output artifact'
    type: string
  version:
    required: false
    description: 'Version used to tag the artifact'
    type: string
  aws_access_key_id:
    required: true
    description: 'AWS access key ID'
  aws_secret_access_key:
    required: true
    description: 'AWS secret access key'

runs:
  using: 'composite'
  # Deploy:
  steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ inputs.aws_access_key_id }}
        aws-secret-access-key: ${{ inputs.aws_secret_access_key }}
        aws-region: ap-southeast-1
    - name: Register task definitions and deploy to service
      run: |
        aws ecs register-task-definition --cli-input-yaml file://ecs/${{ inputs.name }}-task-def.yaml
        aws ecs update-service --cluster arn:aws:ecs:ap-southeast-1:704871211513:cluster/picky-ecs-development-cluster --service ${{ inputs.name }} --task-definition ${{ inputs.name }}-task-def
      shell: bash
